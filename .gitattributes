# Normalize line endings: store LF in the repo, check out native locally.
# Keeps the repo consistent across Windows/macOS/Linux and CI.
* text=auto eol=lf

# Treat binary assets as binary (no EOL conversion).
*.gif binary
*.png binary
*.jpg binary
*.ico binary
